with open('input.txt', 'r', encoding = 'utf-8') as f:
    a = f.readlines()
k = [line for line in a if len(line) > 20]

with open('output.txt', 'w', encoding = 'utf-8') as file:
    file.writelines(k)
    
